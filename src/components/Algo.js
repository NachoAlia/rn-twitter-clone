import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Algo = () => {
  return (
    <View>
      <Text>Algo</Text>
    </View>
  );
};

export default Algo;

const styles = StyleSheet.create({});
